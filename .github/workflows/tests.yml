jobs:
  unit-tests:
    steps:
    - name: "🧪 Run unit tests"
      run: npx jest --config=jest.unit.config.mjs

  e2e-tests:
    steps:
    - name: "🌐 Run E2E tests"
      run: npx jest --config=jest.e2e.config.cjs