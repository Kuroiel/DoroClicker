{
  "scripts": {
    "test": "if [ \"$CI\" = \"true\" ]; then jest; else echo 'Tests skipped locally'; exit 0; fi"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}